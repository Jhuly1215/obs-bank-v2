[auth.ldap]
enabled = true
config_file = /etc/grafana/ldap.toml
allow_sign_up = true

[users]
auto_assign_org = true
auto_assign_org_role = Viewer

[server]
# Grafana 10+ expande estas variables automáticamente desde el entorno
domain = ${DOMAIN_NAME}
root_url = ${GRAFANA_URL}